import React from "react";
import Header from "../../components/header/header";

const Home = ({ theme }) => {
  return (
    <div>
      <Header theme={theme} />
    </div>
  );
};

export default Home;
